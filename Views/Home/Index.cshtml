<<<<<<< HEAD
@{
    ViewData["Title"] = "Головна";
}

<style>
    /* ─── HERO ─── */
    .hero {
        position: relative;
        overflow: hidden;
        padding: 100px 0 80px;
        text-align: center;
    }
    .hero-glow {
        position: absolute;
        inset: 0;
        background:
            radial-gradient(ellipse 60% 50% at 50% -10%, rgba(79,138,255,.18), transparent),
            radial-gradient(ellipse 40% 40% at 80% 60%, rgba(162,89,255,.12), transparent);
        pointer-events: none;
    }
    .hero-badge {
        display: inline-flex;
        align-items: center;
        gap: 8px;
        background: rgba(79,138,255,.12);
        border: 1px solid rgba(79,138,255,.3);
        border-radius: 99px;
        padding: 6px 16px;
        font-size: .8rem;
        color: var(--clr-accent);
        font-weight: 600;
        margin-bottom: 28px;
        letter-spacing: .04em;
    }
    .hero h1 {
        font-family: var(--font-display);
        font-size: clamp(2.4rem, 6vw, 4.2rem);
        font-weight: 900;
        line-height: 1.08;
        letter-spacing: -.03em;
        margin-bottom: 24px;
    }
    .hero h1 .grad {
        background: linear-gradient(135deg, var(--clr-accent), var(--clr-accent2));
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        background-clip: text;
    }
    .hero p {
        color: var(--clr-muted);
        font-size: 1.1rem;
        max-width: 560px;
        margin: 0 auto 40px;
        line-height: 1.75;
    }
    .hero-actions {
        display: flex;
        gap: 14px;
        justify-content: center;
        flex-wrap: wrap;
    }
    .hero-stats {
        display: flex;
        justify-content: center;
        gap: 48px;
        margin-top: 72px;
        flex-wrap: wrap;
    }
    .hero-stat {
        text-align: center;
    }
    .hero-stat-num {
        font-family: var(--font-display);
        font-size: 2rem;
        font-weight: 900;
        background: linear-gradient(135deg, var(--clr-accent), var(--clr-accent2));
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        background-clip: text;
    }
    .hero-stat-label { color: var(--clr-muted); font-size: .85rem; margin-top: 4px; }

    /* ─── SUBJECTS ─── */
    .section { padding: 72px 0; }
    .section-label {
        font-size: .75rem;
        font-weight: 700;
        letter-spacing: .12em;
        text-transform: uppercase;
        color: var(--clr-accent);
        margin-bottom: 12px;
    }
    .section h2 {
        font-family: var(--font-display);
        font-size: clamp(1.6rem, 3vw, 2.4rem);
        font-weight: 700;
        margin-bottom: 8px;
    }
    .section .subtitle {
        color: var(--clr-muted);
        font-size: 1rem;
        margin-bottom: 48px;
    }

    .subject-card {
        background: var(--clr-card);
        border: 1px solid var(--clr-border);
        border-radius: var(--radius);
        padding: 28px 24px;
        text-decoration: none;
        color: var(--clr-text);
        display: block;
        transition: all .2s ease;
        position: relative;
        overflow: hidden;
    }
    .subject-card::before {
        content: '';
        position: absolute;
        inset: 0;
        opacity: 0;
        transition: opacity .2s;
    }
    .subject-card:hover { transform: translateY(-4px); color: var(--clr-text); }
    .subject-card:hover::before { opacity: 1; }

    .subject-card.math  { --sc: #4f8aff; } .subject-card.math::before  { background: radial-gradient(circle at 0% 0%, rgba(79,138,255,.08), transparent 60%); }
    .subject-card.ukr   { --sc: #22c55e; } .subject-card.ukr::before   { background: radial-gradient(circle at 0% 0%, rgba(34,197,94,.08),  transparent 60%); }
    .subject-card.hist  { --sc: #f59e0b; } .subject-card.hist::before  { background: radial-gradient(circle at 0% 0%, rgba(245,158,11,.08), transparent 60%); }
    .subject-card.bio   { --sc: #a259ff; } .subject-card.bio::before   { background: radial-gradient(circle at 0% 0%, rgba(162,89,255,.08), transparent 60%); }
    .subject-card.phys  { --sc: #06b6d4; } .subject-card.phys::before  { background: radial-gradient(circle at 0% 0%, rgba(6,182,212,.08),  transparent 60%); }
    .subject-card.chem  { --sc: #f43f5e; } .subject-card.chem::before  { background: radial-gradient(circle at 0% 0%, rgba(244,63,94,.08),  transparent 60%); }

    .subject-icon {
        width: 52px; height: 52px;
        border-radius: 12px;
        display: flex; align-items: center; justify-content: center;
        font-size: 1.5rem;
        margin-bottom: 16px;
        background: rgba(255,255,255,.05);
        border: 1px solid rgba(255,255,255,.08);
    }
    .subject-card:hover .subject-icon { background: rgba(var(--sc-rgb), .15); }
    .subject-card h3 { font-family: var(--font-display); font-size: 1rem; font-weight: 700; margin-bottom: 6px; }
    .subject-card p  { color: var(--clr-muted); font-size: .83rem; margin: 0; }
    .subject-card .subject-count {
        position: absolute; top: 20px; right: 20px;
        font-size: .75rem; color: var(--clr-muted);
        background: var(--clr-surface);
        border: 1px solid var(--clr-border);
        padding: 2px 10px; border-radius: 99px;
    }

    /* ─── HOW IT WORKS ─── */
    .steps { display: flex; gap: 0; position: relative; }
    .steps::before {
        content: '';
        position: absolute;
        top: 28px; left: 28px; right: 28px; height: 1px;
        background: linear-gradient(90deg, var(--clr-accent), var(--clr-accent2));
        opacity: .3;
    }
    .step { flex: 1; text-align: center; padding: 0 20px; position: relative; }
    .step-num {
        width: 56px; height: 56px;
        border-radius: 50%;
        background: var(--clr-card);
        border: 2px solid var(--clr-border);
        display: flex; align-items: center; justify-content: center;
        font-family: var(--font-display); font-weight: 900; font-size: 1rem;
        margin: 0 auto 20px;
        position: relative; z-index: 1;
        transition: all .2s;
    }
    .step:hover .step-num {
        border-color: var(--clr-accent);
        background: rgba(79,138,255,.12);
        color: var(--clr-accent);
    }
    .step h4 { font-family: var(--font-display); font-weight: 700; font-size: .95rem; margin-bottom: 8px; }
    .step p  { color: var(--clr-muted); font-size: .85rem; }

    /* ─── FEATURES ─── */
    .feature-item { display: flex; gap: 20px; align-items: flex-start; }
    .feature-icon {
        width: 48px; height: 48px; flex-shrink: 0;
        border-radius: var(--radius-sm);
        background: rgba(79,138,255,.12);
        border: 1px solid rgba(79,138,255,.25);
        display: flex; align-items: center; justify-content: center;
        font-size: 1.25rem; color: var(--clr-accent);
    }
    .feature-item h5 { font-weight: 600; margin-bottom: 4px; }
    .feature-item p  { color: var(--clr-muted); font-size: .875rem; margin: 0; }

    /* ─── CTA ─── */
    .cta-block {
        background: var(--clr-card);
        border: 1px solid var(--clr-border);
        border-radius: calc(var(--radius) * 1.5);
        padding: 64px 48px;
        text-align: center;
        position: relative;
        overflow: hidden;
        margin: 0 0 80px;
    }
    .cta-block::before {
        content: '';
        position: absolute;
        inset: 0;
        background: radial-gradient(ellipse 70% 60% at 50% 0%, rgba(79,138,255,.1), transparent);
        pointer-events: none;
    }
    .cta-block h2 { font-family: var(--font-display); font-size: clamp(1.5rem, 3vw, 2.2rem); font-weight: 900; margin-bottom: 16px; }
    .cta-block p  { color: var(--clr-muted); font-size: 1rem; margin-bottom: 32px; }

    @@media (max-width: 768px) {
        .steps { flex-direction: column; gap: 28px; }
        .steps::before { display: none; }
        .hero-stats { gap: 28px; }
    }
</style>

<!-- ═══ HERO ═══ -->
<section class="hero">
    <div class="hero-glow"></div>
    <div class="container position-relative">
        <div class="hero-badge">
            <i class="bi bi-stars"></i> Платформа #1 для підготовки до НМТ
        </div>
        <h1>
            Підготуйся до ЗНО/<br class="d-md-none"><span class="grad">НМТ</span> на 200 балів
        </h1>
        <p>Тисячі тестів, детальна аналітика, робота над помилками — все що потрібно для впевненого результату.</p>
        <div class="hero-actions">
            <a href="/Account/Register" class="btn-zno btn-primary-zno" style="padding:13px 32px;font-size:1rem">
                <i class="bi bi-rocket-takeoff"></i> Почати безкоштовно
            </a>
            <a href="/Tests" class="btn-zno btn-ghost-zno" style="padding:13px 32px;font-size:1rem">
                <i class="bi bi-play-circle"></i> Переглянути тести
            </a>
        </div>
        <div class="hero-stats">
            <div class="hero-stat">
                <div class="hero-stat-num">10 000+</div>
                <div class="hero-stat-label">питань у базі</div>
            </div>
            <div class="hero-stat">
                <div class="hero-stat-num">6</div>
                <div class="hero-stat-label">предметів</div>
            </div>
            <div class="hero-stat">
                <div class="hero-stat-num">500+</div>
                <div class="hero-stat-label">тестів</div>
            </div>
            <div class="hero-stat">
                <div class="hero-stat-num">100%</div>
                <div class="hero-stat-label">безкоштовно</div>
            </div>
        </div>
    </div>
</section>

<!-- ═══ SUBJECTS ═══ -->
<section class="section" style="background:var(--clr-surface); border-top:1px solid var(--clr-border); border-bottom:1px solid var(--clr-border)">
    <div class="container">
        <div class="section-label">Предмети</div>
        <h2>Обери свій предмет</h2>
        <p class="subtitle">Тести по всіх основних дисциплінах НМТ</p>
        <div class="row g-3">
            <div class="col-md-4 col-6">
                <a href="/Tests?subject=math" class="subject-card math">
                    <span class="subject-count">1 200 питань</span>
                    <div class="subject-icon">📐</div>
                    <h3>Математика</h3>
                    <p>Алгебра, геометрія, тригонометрія</p>
                </a>
            </div>
            <div class="col-md-4 col-6">
                <a href="/Tests?subject=ukr" class="subject-card ukr">
                    <span class="subject-count">980 питань</span>
                    <div class="subject-icon">📝</div>
                    <h3>Українська мова</h3>
                    <p>Граматика, орфографія, пунктуація</p>
                </a>
            </div>
            <div class="col-md-4 col-6">
                <a href="/Tests?subject=hist" class="subject-card hist">
                    <span class="subject-count">750 питань</span>
                    <div class="subject-icon">🏛</div>
                    <h3>Історія України</h3>
                    <p>Від давнини до сучасності</p>
                </a>
            </div>
            <div class="col-md-4 col-6">
                <a href="/Tests?subject=bio" class="subject-card bio">
                    <span class="subject-count">860 питань</span>
                    <div class="subject-icon">🧬</div>
                    <h3>Біологія</h3>
                    <p>Клітина, еволюція, екологія</p>
                </a>
            </div>
            <div class="col-md-4 col-6">
                <a href="/Tests?subject=phys" class="subject-card phys">
                    <span class="subject-count">620 питань</span>
                    <div class="subject-icon">⚛️</div>
                    <h3>Фізика</h3>
                    <p>Механіка, електрика, оптика</p>
                </a>
            </div>
            <div class="col-md-4 col-6">
                <a href="/Tests?subject=chem" class="subject-card chem">
                    <span class="subject-count">540 питань</span>
                    <div class="subject-icon">🧪</div>
                    <h3>Хімія</h3>
                    <p>Органічна та неорганічна хімія</p>
                </a>
            </div>
        </div>
    </div>
</section>

<!-- ═══ HOW IT WORKS ═══ -->
<section class="section">
    <div class="container">
        <div class="text-center mb-5">
            <div class="section-label">Як це працює</div>
            <h2>4 кроки до 200 балів</h2>
        </div>
        <div class="steps">
            <div class="step">
                <div class="step-num">01</div>
                <h4>Реєстрація</h4>
                <p>Створи акаунт за 30 секунд — безкоштовно</p>
            </div>
            <div class="step">
                <div class="step-num">02</div>
                <h4>Обери предмет</h4>
                <p>Вибери тест з потрібної теми або пройди повний</p>
            </div>
            <div class="step">
                <div class="step-num">03</div>
                <h4>Проходь тести</h4>
                <p>Режим навчання або іспиту з таймером</p>
            </div>
            <div class="step">
                <div class="step-num">04</div>
                <h4>Аналізуй прогрес</h4>
                <p>Дивись графіки, прогноз балу і працюй над помилками</p>
            </div>
        </div>
    </div>
</section>

<!-- ═══ FEATURES ═══ -->
<section class="section" style="background:var(--clr-surface); border-top:1px solid var(--clr-border); border-bottom:1px solid var(--clr-border)">
    <div class="container">
        <div class="row align-items-center g-5">
            <div class="col-lg-5">
                <div class="section-label">Переваги</div>
                <h2>Все необхідне для успішної підготовки</h2>
                <p style="color:var(--clr-muted); margin-top:16px; line-height:1.8">
                    ZNOway — це не просто база питань. Це повноцінна система підготовки з аналітикою та персональними рекомендаціями.
                </p>
            </div>
            <div class="col-lg-7">
                <div class="d-flex flex-column gap-4">
                    <div class="feature-item">
                        <div class="feature-icon"><i class="bi bi-clock"></i></div>
                        <div>
                            <h5>Режим іспиту з таймером</h5>
                            <p>Симулюй реальний НМТ — з обмеженням часу та форматом відповідей як на справжньому іспиті.</p>
                        </div>
                    </div>
                    <div class="feature-item">
                        <div class="feature-icon" style="background:rgba(162,89,255,.12);border-color:rgba(162,89,255,.25);color:var(--clr-accent2)"><i class="bi bi-graph-up"></i></div>
                        <div>
                            <h5>Прогноз балу 100–200</h5>
                            <p>Бачиш свій прогрес у шкалі НМТ — знаєш точно, на який результат можеш розраховувати.</p>
                        </div>
                    </div>
                    <div class="feature-item">
                        <div class="feature-icon" style="background:rgba(34,197,94,.12);border-color:rgba(34,197,94,.25);color:var(--clr-success)"><i class="bi bi-arrow-repeat"></i></div>
                        <div>
                            <h5>Робота над помилками</h5>
                            <p>Система запам'ятовує де ти помилився і повертає ці питання для повторення.</p>
                        </div>
                    </div>
                    <div class="feature-item">
                        <div class="feature-icon" style="background:rgba(245,158,11,.12);border-color:rgba(245,158,11,.25);color:var(--clr-warn)"><i class="bi bi-bookmark-star"></i></div>
                        <div>
                            <h5>Обране</h5>
                            <p>Зберігай складні питання в обране щоб повернутися до них пізніше.</p>
                        </div>
                    </div>
=======
﻿@{
    ViewData["Title"] = "Головна";
}

@if (User.Identity != null && User.Identity.IsAuthenticated)
{
    <div class="row">
        <div class="col-12 text-center mb-5">
            <h1 class="display-5 fw-bold">Ласкаво просимо, @User.Identity.Name! 👋</h1>
            <p class="lead text-muted">Обирай тест і починай підготовку до ЗНО</p>
        </div>
    </div>

    <div class="row g-4 justify-content-center">
        <div class="col-md-4">
            <div class="card h-100 shadow-sm border-0">
                <div class="card-body text-center p-4">
                    <div class="display-4 mb-3">📖</div>
                    <h5 class="card-title">Тести</h5>
                    <p class="card-text text-muted">Переглянь доступні тести та почни підготовку</p>
                    <a asp-controller="Tests" asp-action="Index" class="btn btn-primary">Перейти до тестів</a>
                </div>
            </div>
        </div>
        <div class="col-md-4">
            <div class="card h-100 shadow-sm border-0">
                <div class="card-body text-center p-4">
                    <div class="display-4 mb-3">📚</div>
                    <h5 class="card-title">Предмети</h5>
                    <p class="card-text text-muted">Переглянь всі предмети та теми</p>
                    <a asp-controller="Subjects" asp-action="Index" class="btn btn-outline-primary">Предмети</a>
                </div>
            </div>
        </div>
        <div class="col-md-4">
            <div class="card h-100 shadow-sm border-0">
                <div class="card-body text-center p-4">
                    <div class="display-4 mb-3">🏆</div>
                    <h5 class="card-title">Мої результати</h5>
                    <p class="card-text text-muted">Переглянь свою статистику та прогрес</p>
                    <a asp-controller="UserResults" asp-action="Index" class="btn btn-outline-success">Результати</a>
>>>>>>> 090e2cd928202a10759c36d84e6664f30602fc76
                </div>
            </div>
        </div>
    </div>
<<<<<<< HEAD
</section>

<!-- ═══ CTA ═══ -->
<section class="section">
    <div class="container">
        <div class="cta-block">
            <h2>Готовий до 200 балів?</h2>
            <p>Реєструйся зараз — це безкоштовно та займає менше хвилини</p>
            <a href="/Account/Register" class="btn-zno btn-primary-zno" style="padding:14px 40px;font-size:1.05rem">
                <i class="bi bi-rocket-takeoff"></i> Розпочати підготовку
            </a>
        </div>
    </div>
</section>
=======
}
else
{
    <div class="row justify-content-center mt-5">
        <div class="col-md-6 text-center">
            <h1 class="display-4 fw-bold mb-3">📚 ZNOWay</h1>
            <p class="lead mb-4">Платформа для підготовки до ЗНО. Тести, теми, результати — все в одному місці.</p>
            <a asp-controller="Account" asp-action="Register" class="btn btn-success btn-lg me-3">
                Зареєструватись
            </a>
            <a asp-controller="Account" asp-action="Login" class="btn btn-outline-primary btn-lg">
                Увійти
            </a>
        </div>
    </div>
}
>>>>>>> 090e2cd928202a10759c36d84e6664f30602fc76
